# 40 — Live token page (streaming)

## X (short)
Token pages on ArcTools now stream.

Every swap on Arc lands on the chart, the trade list and the price within a second of the block. No refresh, no polling. Powered by our own Arc node.

arctools.fun

## X (long, Premium)
Speed pass on arctools.fun, shipped today:

- Live token pages: swaps stream straight from our indexer to your browser (server-sent events). Candle, price and trade list update the moment the block lands. Fallback polling every 5 s if the stream drops.
- Own Arc node is now the primary RPC for the site, the index, the sniper and the relay. Public RPCs are backup only. Chart + trades API answer in ~0.3 s.
- 26 launchpads recognised on-chain, including Klik, Minara, pools.trade, Tolly, Lift and Arguspad factories that used to hide under "Uniswap V3 / V4".
- New launches get symbol, name, logo and socials on first sight. 1,498 stubs backfilled.
- USDC quote pools no longer show up as tokens; no more 3.99e+26 market caps.

Green "LIVE" badge above the chart = you are on the stream.

arctools.fun

## Telegram
Token pages now stream live: every swap hits the chart, price and trade list within a second of the block. Own Arc node as primary RPC, 26 launchpads recognised on-chain (Klik, Minara, pools.trade, Tolly, Lift, Arguspad...), symbols and logos on new launches from the first block. https://arctools.fun
